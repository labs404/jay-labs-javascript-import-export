// (part 1) export a function.
export let multiplyFunction = function(a, b) {
  return a * b;
};

///////////////////////////////////////////////////////////////////////////////////////////////

// (part 2) export a class. (in classes each first letter is capitalized)
export class MathFunctions {
  sum(a, b) {
    return a + b;
  }
  multiply(a, b) {
    return a * b;
  }
}

///////////////////////////////////////////////////////////////////////////////////////////////

// (part 3) export several functions.
export let multiplyFunctionTwo = function(a, b) {
  return a * b;
};

export let differenceFunction = function(a, b) {
  return a - b;
};

///////////////////////////////////////////////////////////////////////////////////////////////
